def is_subseq(s1, s2)
    j = 0
    i = 0
    m = len(s1)
    n = len(s2)
    while(im and jn) 
        if(s1[i] == s2[j])
            i += 1
            j += 1
        else
            j += 1
    
    return i == m
